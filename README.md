# KNN-Aglorithm
The KNN algorithm works by finding the K closest data points (neighbors) to a given input and predicting the output based on majority voting (for classification) or averaging (for regression).
Author - Megha Kundale 
